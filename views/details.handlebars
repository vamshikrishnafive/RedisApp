<div class="card">
    <div class="card-header">
        Name: {{user.first_name}} {{user.last_name}} Id: {{user.id}}
    </div>
    <div class="card-body">
        <div class="blockquote mb-0">
            <ulclass="list">
                <li class="list-group-item">
                    <strong> Email: </strong> {{user.email}}
                </li>
                <li class="list-group-item">
                    <strong> Phone: </strong> {{user.phone}}
                </li>
                </ul>
                <a class="col md-2 btn btn-primary pull-left" href='/'>Back</a>
                <form class="col md-2 btn btn-danger pull-let" method="POST"
                    action="/user/delete/{{user.id}}?_method=DELETE">
                    <input type="submit" value="Delete" >
                </form>
        </div>
    </div>
</div>